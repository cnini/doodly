import { Text, View } from "react-native"
import { styles } from "./CartStyleSheet"

export const Cart = () => {
    return (
        <View style={styles.cartContainer}>
            <Text>Cart.js</Text>
        </View>
    )
}