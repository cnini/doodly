import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    cartContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },

    backgroundImage: {
        position: 'absolute',
        width: '100%',
        height: '100%',
    },

    orderCardsTitle: {
        fontSize: 22,
        fontWeight: 'bold',
        marginBottom: 10
    },

    orderCardsContainer: {
        marginBottom: 50
    },
})